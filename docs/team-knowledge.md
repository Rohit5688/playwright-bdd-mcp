# TestForge MCP — Team Knowledge Base

**Version**: 1.0.0
**Total Rules**: 14

| # | Pattern | Solution | Tags | Learned |
|---|---------|----------|------|---------|
| 1 | TestForge API docs (AgentBrain.mdx, PageController.mdx, SandboxEngine.mdx) contained wrong page-agent monorepo class references — generatePlan(), FlatDomTree, MacroToolInput — which don't exist in TestForge | Rewrote all three files: AgentBrain → ContextManager (DOM history, structural map, token counters), PageController → Locator strategy priority table + iframe/storageState patterns, SandboxEngine → forge.api namespace docs + constraints table + code patterns. Always cross-reference actual source before writing API docs. | documentation, api-reference, correctness | 2026-04-16 |
| 2 | Custom wrapper projects: SmartDomExtractor emits page.getByRole() strings which conflict with wrapper-specific APIs | SmartDomExtractor now emits dual output: (1) locator = Playwright API string for standard projects; (2) selectorArgs = { method, role?, name?, testId?, placeholder? } for custom-wrapper adapters. When customWrapperPackage is set in generate_gherkin_pom_test_suite, a CUSTOM WRAPPER SELECTOR RULES block is injected into memoryPrompt that tells the LLM to consume selectorArgs and call wrapper methods instead of copying the locator string. | custom-wrapper, selectorArgs, inspect_page_dom, generate_gherkin_pom_test_suite, SmartDomExtractor | 2026-04-17 |
| 3 | LLMs ignore prompt instructions to use getByRole() and generate XPath or CSS class selectors anyway | Semantic linter gate added to validate_and_write (LocatorAuditService.lintInlineContent). BLOCKS write with isError:true when XPath in locator(), page.$(), or CSS .class/#id selectors are found. Non-blocking warnings for generic page.locator() strings. This is code-level enforcement — runs before staging, regardless of what the LLM generated. | semantic-linter, validate_and_write, XPath, CSS-selector, enforcement | 2026-04-17 |
| 4 | TestGenerationService generates brittle selectors because it receives Markdown DOM output with no structured locator field references | Added domJsonContext optional param to generatePromptInstruction(). When caller passes JSON from inspect_page_dom(returnFormat:'json'), a DOM ELEMENT REFERENCE block is injected into the prompt with each element's id, role, text, and locator (a ready-to-use Playwright API string like page.getByRole(...)). The LLM is instructed to copy locator verbatim. Wired end-to-end: index.ts schema → args destructuring → generatePromptInstruction call. Rule #2 in mandatory requirements also updated to explicitly name forbidden patterns (XPath, CSS class selectors). | TestGenerationService, domJsonContext, locator-injection, prompt-engineering | 2026-04-17 |
| 5 | LLM creates monolithic Page Object (EcommercePage) with all pages/screens collapsed into one class and one file — ignores multi-file POM instruction | Two-layer fix: (1) Rule 30 added to TestGenerationService prompt — CRITICAL ONE CLASS PER FILE — with emoji wrong/correct example. jsonPageObjects example expanded to 3 entries (HomePage, ProductPage, CartPage). (2) MONOLITHIC POM DETECTION GATE added to validate_and_write — uses regex /export\s+(?:default\s+)?class\s+(\w+)/g on each .ts file, blocks write with isError:true if any file has >1 class. Error lists offending files + class names + split instructions. Runs after semantic linter gate, before staging. | monolithic-pom, validate_and_write, structural-enforcement, one-class-per-file, Gemini-Flash-3 | 2026-04-17 |
| 6 | Graphify structural brain generation extracts noise from Playwright traces and minified test assets (e.g., `playwright-report/trace/assets`) leading to disconnected "Thin communities" and corrupted "God Nodes" (e.g. `rv()`). | Filter AST extraction explicitly to the `src/` directory directly within the Python script (`build_appforge_graph.py`) preventing the parsing of test outputs, example traces, and `.d.ts` declaration files that lack architectural body. | ast, graphify, structural-brain | 2026-04-19 |
| 7 | bad_locator | use text |  | 2026-04-19 |
| 8 | Migrating MCP servers from deprecated SSEServerTransport to StreamableHTTPServerTransport with Express. Express route 404s after build. | 1. Collapsed dual-endpoint SSE pattern (GET /sse + POST /message) into a single stateless POST /mcp endpoint. 
2. Use StreamableHTTPServerTransport with sessionIdGenerator: undefined to enforce modern stateless behavior. 
3. Resolve TypeScript 'exactOptionalPropertyTypes' errors by omitting optional transport config keys. 
4. Work around SDK 'onclose' type mismatch in Express by casting the response object 'as any' when passing to handleRequest. 
5. CRITICAL: If new routes 404 despite clean code, perform a clean build (rm -r dist && tsc) and verify with required headers: 'Content-Type: application/json' AND 'Accept: application/json, text/event-stream'. | mcp, migration, transport, express, typescript | 2026-04-20 |
| 9 | Enterprise Knowledge Management: Balancing Tenant Isolation with Shared Learning (Knowledge Flywheel) | Implement a Three-Tier Learning Architecture:
1. **Tier 1 (Transient)**: User-specific local session patterns.
2. **Tier 2 (Tenant)**: Team-wide verified patterns stored in /.TestForge/tenants/{tenant_id}/learning.json.
3. **Tier 3 (Global/Central)**: The 'Global Brain' containing de-duplicated, promoted patterns from all tenants.

Merging Service Strategy:
- **Fingerprinting**: Normalize the 'issuePattern' and 'solution' via structural hashing.
- **De-duplication**: Compare new tenant learnings against the Global Brain to avoid redundant entries.
- **Auto-Promotion**: Implement a confidence-based promotion system where patterns with high utility across multiple users are elevated to the Central Knowledge Base.
- **Privacy Masking**: Before merging into the Global Brain, strip PII (Personal Identifiable Information) and site-specific URLs unless they are relevant for the structural fix. | architecture, multitenancy, learning-service, enterprise | 2026-04-20 |
| 10 | Managing Knowledge Decay and Duplication in a Federated AI Brain | Implement a Knowledge Hygiene Service with the following controls:
1. **Semantic Merging**: Use LLM-based semantic comparison to group similar issues into 'Canonical Problems'.
2. **Success-Based Decay**: Track usage metrics (success_count vs failure_count). Apply a decay function to entries that haven't succeeded in the last X attempts.
3. **Version Sensitivity**: Tag all learned patterns with SDK/Environment versions. Prefer 'Recent-SDK' knowledge over 'Legacy-SDK' knowledge during tool execution.
4. **Conflict Mediation**: If two tenants suggest different fixes for the same structural issue, the 'Federated Service' monitors their respective success rates and promotes the 'Winning' solution to the Global Brain.
5. **Periodic Pruning**: Implement a background cron job ('The Gardener') to archive entries with zero usage in 90 days. | knowledge-management, maintenance-hygiene, enterprise-scaling, brain-lifecycle | 2026-04-20 |
| 11 | Ensuring Knowledge Base Resilience and Multi-Tenant Shadowing Patterns | 1. **Shadowing Lookup**: Implement a cascading lookup service: Tenant-Local -> Organization-Group -> Global. User corrections always create a 'Shadow Entry' in the local pool first.
2. **Central Sync Reconciliation**: The 'Gardener' service compares local shadow entries against global patterns. If a global pattern consistently fails/is overridden across multiple tenants, it is flagged for 'Central Update' or 'Central Delete'.
3. **Write-Ahead Logging (WAL)**: All knowledge updates are first written to a 'transaction.log' before being committed to the main JSON file.
4. **Automated Snapshots**: Implement a standard backup service that creates timestamped GZipped snapshots of the entire /.TestForge directory:
   - Daily: Full snapshot.
   - Hourly: Incremental change logs.
5. **Crash Recovery**: on 'server_start', the system checks the transaction log. If it finds an uncommitted entry from a crash, it replays the update to ensure state consistency. | resilience, backups, shadowing-patterns, knowledge-sync | 2026-04-20 |
| 12 | Advanced Enterprise Multi-Tenancy: Noisy Neighbors and Secret Isolation | 1. **Concurrency Throttling**: Implement a global 'Semaphore' service that tracks active automation sessions per tenant. Reject/Queue requests exceeding the per-tenant parallel quota.
2. **Tenant-Keyed Vault**: Encrypt sensitive credentials at rest using tenant-specific salts or keys. Resolve secrets through a 'VaultService' that enforces tenant context checks.
3. **Log Attribution**: Inject 'tenantId' into every log entry at the transport middleware level. Use this tag for IAM-based access control on external logging platforms.
4. **Isolated Runners**: For version-sensitive automation, decouple the 'MCP Server' (logic) from the 'Test Runner' (execution). Spwan runners in isolated environments (cgroups/containers) matching the tenant's required SDK version. | governance, security, observability, concurrency-management | 2026-04-20 |
| 13 | Iterative Multi-Tenant Stress Testing and Regression Patterns | 1. **Isolation Probing**: In every cycle, execute a 'Data Leak' test where User A reads from a projectRoot shared with User B, but with different tenant contexts. Verify partitioned brain integrity.
2. **Saturation Point Analysis**: During 5-user stress tests, monitor 'Context Switching Latency' and 'Event Loop Lag'. If lag > 100ms, implement further request queuing or CPU affinity.
3. **Synchronicity Verification**: After a 'Knowledge Promotion' event to the Global Brain, immediately trigger a retest across all 5 clients to ensure 'Shadow Overrides' are correctly respected or cleared.
4. **Crash-Consistency Loop**: During active multi-user runs, trigger a 'SIGKILL' on the MCP process. Verify the 'Write-Ahead Logs' (WAL) correctly replay and restore the knowledge base on restart without corrupting tenant sub-directories. | testing-strategy, regression-cycles, stress-testing, isolation-verification | 2026-04-20 |
| 14 | Refactoring a flat service directory into a Domain-Driven AppForge-compliant architecture without breaking imports | 1. Create domain folders early (analysis, audit, config, execution, generation, io, nav, setup, system, dom). 2. Create an automated script (`reorganize.js`) instead of manual file-by-file moving to ensure atomicity. 3. Script must execute `fs.renameSync` for all file artifacts (.ts, .d.ts, .js, .map) associated with a service simultaneously. 4. Script must parse and correctly rewrite `import` statements globally (ServiceContainer.ts, tools/, tests/, utils/) shifting paths backwards by one directory (`../` to `../../`). 5. Run `npx tsc --noEmit` locally, fixing any edge cases natively until passing. Never commit midway without 0 TypeScript compilation errors. | architecture, refactoring, domain-driven | 2026-04-20 |
