node_modules/
dist/
.features-gen/
test-results/
playwright-report/
*.env
.env.*
!.env.example
test-data/users.*.json
# Environment secrets
.env.local

# Environment secrets
.env.staging

# Environment secrets
.env.prod

# User credential stores (never commit)
test-data/users.local.json

# Local environment / API keys (never commit)
.env

# User credential stores (never commit)
test-data/users.staging.json

# User credential stores (never commit)
test-data/users.prod.json
